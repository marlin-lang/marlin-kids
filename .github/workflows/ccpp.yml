name: marlin-kids

on: [push]

jobs:
  build:

    runs-on: macOS-latest
    
    steps:
    - uses: actions/checkout@master
    - name: build
      run: |
        mkdir build
        cd build
        cmake -GXcode ..
        cmake --build .
    - name: test
      run: |
        cd build
        ctest
